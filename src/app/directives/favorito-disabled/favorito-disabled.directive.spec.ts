import { FavoritoDisabledDirective } from './favorito-disabled.directive';

describe('FavoritoDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoritoDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
